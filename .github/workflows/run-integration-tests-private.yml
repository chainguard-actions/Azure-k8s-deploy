name: Cluster Integration Tests - private cluster
on:
   pull_request:
      branches:
         - 'releases/*'
   push:
      branches:
         - main
   workflow_dispatch:

jobs:
   run-integration-test:
      name: Run Minikube Integration Tests
      runs-on: ubuntu-22.04
      env:
         KUBECONFIG: /home/runner/.kube/config
         NAMESPACE: test-${{ github.run_id }}
      permissions:
         contents: read
         id-token: write
      steps:
         - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2

         - name: Install dependencies
           run: |
              rm -rf node_modules/
              npm install

         - name: Build
           run: npm run build

         - name: Azure login
           uses: azure/login@a457da9ea143d694b1b9c7c869ebb04ebe844ef5 # v2.3.0
           with:
              client-id: ${{ secrets.AZURE_CLIENT_ID }}
              tenant-id: ${{ secrets.AZURE_TENANT_ID }}
              subscription-id: ${{ secrets.AZURE_SUBSCRIPTION_ID }}

         - uses: Azure/setup-kubectl@776406bce94f63e41d621b960d78ee25c8b76ede # v4.0.1
           name: Install Kubectl

         - name: Create private AKS cluster and set context
           env:
              NS: ${{ env.NAMESPACE }}
           run: |
              set +x
              # create cluster
              az group create --location eastus2 --name "$NS"
              az aks create --name "$NS" --resource-group "$NS" --enable-private-cluster --generate-ssh-keys
              az aks get-credentials --resource-group "$NS" --name "$NS"

         - name: Create namespace to run tests
           env:
              NS: ${{ env.NAMESPACE }}
           run: |
              az aks command invoke --resource-group "$NS" --name "$NS" --command "kubectl create ns $NS"

         - uses: actions/setup-python@a309ff8b426b58ec0e2a45f0f869d46889d02405 # 6.2.0
           name: Install Python
           with:
              python-version: '3.x'

         - name: Executing deploy action for pod
           uses: ./
           with:
              namespace: ${{ env.NAMESPACE }}
              images: nginx:1.14.2
              manifests: |
                 test/integration/manifests/test.yml
                 test/integration/manifests/test2.yml
              action: deploy
              private-cluster: true
              resource-group: ${{ env.NAMESPACE }}
              name: ${{ env.NAMESPACE }}

         - name: Checking if deployments and services were created
           env:
              NS: ${{ env.NAMESPACE }}
           run: |
              python test/integration/k8s-deploy-test.py "private=$NS" "namespace=$NS" kind=Deployment name=nginx-deployment containerName=nginx:1.14.2 labels=app:nginx,workflow:actions.github.com-k8s-deploy,workflowFriendlyName:Cluster_Integration_Tests_-_private_cluster selectorLabels=app:nginx
              python test/integration/k8s-deploy-test.py "private=$NS" "namespace=$NS" kind=Service name=nginx-service labels=workflow:actions.github.com-k8s-deploy,workflowFriendlyName:Cluster_Integration_Tests_-_private_cluster selectorLabels=app:nginx

              python test/integration/k8s-deploy-test.py "private=$NS" "namespace=$NS" kind=Deployment name=nginx-deployment2 containerName=nginx:1.14.2 labels=app:nginx2,workflow:actions.github.com-k8s-deploy,workflowFriendlyName:Cluster_Integration_Tests_-_private_cluster selectorLabels=app:nginx2
              python test/integration/k8s-deploy-test.py "private=$NS" "namespace=$NS" kind=Service name=nginx-service2 labels=workflow:actions.github.com-k8s-deploy,workflowFriendlyName:Cluster_Integration_Tests_-_private_cluster selectorLabels=app:nginx2

         - name: Clean up AKS cluster
           if: ${{ always() }}
           env:
              NS: ${{ env.NAMESPACE }}
           run: |
              echo "deleting AKS cluster and resource group"
              az aks delete --yes --resource-group "$NS" --name "$NS"
              az group delete --resource-group "$NS" --yes
